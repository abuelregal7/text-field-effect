project 'text field effect.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'text field effect' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'TextFieldEffects'
  # Pods for text field effect

  target 'text field effectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'text field effectUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
